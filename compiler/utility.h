#ifndef UTILITY_H
#define UTILITY_H

#include <iostream>
#include <string>
#include <vector>

using namespace std;

string uppercase(string input);
vector<string> split(string line, char delimeter);

#endif
