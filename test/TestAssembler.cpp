#include "TestAssembler.h"

void TestAssembler::test_simple(){
    //QStringList inpu t; */
    // input << "B 10\nADD R4 R5";
    // Assembled* a = assembler.assemble(input, AssemblerConfiguration());
    // qDebug() << a->isAssembled << endl;
    // for(int i = 0; i < a->instructions->size(); i++){
    //     qDebug() << QString::number(a->instructions->at(i),2);
    // }
    // delete a->warningLog;
    // delete a->instructions;
    // delete a->errorLog;
    /* delete a; */
}
